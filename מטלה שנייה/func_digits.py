"""""student: Asaf FitusiID:318763430Assignment no.2program:func_digits""" def count_Digits(n): #I used the code Haio provided us druing the practice session    """    Counts the number of digits in the given number "n".    """    count = 0    while n != 0:        count += 1        n //= 10    return countdef mullEven_Digits(n):#I used the code Haio provided us druing the practice session    """    Multiplies all the even digits in the given number "n".    Returns the product of even digits.    (if there are no even digits m==0)    """    m = 1     while n != 0:        digit = n % 10          if digit % 2 == 0:              m *= digit          n //= 10      return m if m != 1 else 0def sumOdd_Digits(n):#I used the code Haio provided us druing the practice session    """    Sums all the odd digits in the given number "n".    Returns the sum of odd digits.    """    s = 0    while n != 0:        if (n % 10) % 2 == 1:  # Check if the digit is odd            s += n % 10        n //= 10    return sdef func_digits(n):    """    Determines whether the number of digits in "n" is even or odd.    If the number of digits is even, returns the product of even digits.    If the number of digits is odd, returns the sum of odd digits.    """    x = count_Digits(n)    if x % 2 == 0:        return mullEven_Digits(n)    else:        return sumOdd_Digits(n)def read_numbers():    """    Reads positive integers from the user until a non-positive number is entered.    For each number, calculates a result using "func_digits" and tracks the number    with the highest result.    Returns the number with the highest result, or None if no valid numbers were entered.    """    max_result = 0    max_result_num = None    num = int(input("Enter positive integers (0 or less to stop): "))    while num > 0:        result = func_digits(num)        print(result)        if result > max_result:            max_result = result            max_result_num = num        num = int(input("Enter positive integers (0 or less to stop): "))    if max_result_num != 0:        return max_result_num    else:        return Nonedef main():    """   this function  execute the program.    calls "read_number" and prints the number with the maximum result.    """    Last_Result = read_numbers()    print("Number with maximal sum:", Last_Result)main()